set(INSITEX_CLIENT_TARGET_NAME ${PROJECT_NAME}Client)

file(GLOB_RECURSE INSITEX_APP1_SOURCES *.cpp)

add_executable(${PROJECT_NAME}Client ${INSITEX_APP1_SOURCES})

target_link_libraries(${INSITEX_CLIENT_TARGET_NAME} PUBLIC ${PROJECT_NAME}Ipc)